import example.Lists

object teste {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(60); val res$0 = 
  
  Lists.max(List());System.out.println("""res0: Int = """ + $show(res$0))}
}
